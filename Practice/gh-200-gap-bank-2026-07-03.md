# GH-200 — Authored Gap Bank (uncovered Jan-2026 objectives)

**Authored:** 2026-07-03
**Source:** written for this project per `GH-200_Gap_Study_Plan.md` §5 — NOT from GHCertified; every question is original, keyed against `docs.github.com`.
**Scope:** the objectives the GHCertified bank gives zero/thin drillable reps on: 5.8 attestations/SLSA (9), 5.3 script injection (8), 1.9/2.3 YAML anchors (7), 1.14 job summaries (5), 1.7 service containers (6), 4.4 IP allow lists/networking (6), 4.5 runner groups & troubleshooting (6), 3.3 action troubleshooting (5).
**Count:** 52 questions (5 multi-select, 47 single-answer)
**IDs:** Q501–Q552 — disjoint from GHCertified Q001–Q179 so the shared `attempts.jsonl` stays unambiguous. Never merge this file into the GHCertified bank.

**Verification (2026-07-06):** every question independently checked against live `docs.github.com` by four review agents (keys, distractors, ambiguity, URLs). Result: 2 keys corrected before first use (Q521 — Actions rejects YAML merge keys; Q546 — the connectivity self-test is `config.sh --check`, not `run.sh --check`), 1 rewritten for ambiguity (Q520), 4 wordings tightened, all Docs links updated to the post-2025-reorg canonical pages. Re-verify currency if using this bank more than a few months after this date; the most movement-prone facts are attestation availability (GHES had none through 3.21) and the `actions/attest` vs `attest-build-provenance` naming (the latter is now a wrapper).

---

## Q501

What does an artifact attestation establish about a build artifact?

- A. Unfalsifiable provenance: which workflow, repository, and commit produced the artifact
- B. That the artifact contains no known vulnerabilities
- C. That all of the artifact's tests passed
- D. That a reviewer approved the artifact for deployment

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/concepts/security/artifact-attestations

---

## Q502

In addition to `contents: read`, which permissions does a job need to generate build provenance for a binary with `actions/attest-build-provenance`?

- A. `id-token: write`
- B. `attestations: write`
- C. `packages: write`
- D. `actions: write`

**Answer:** A, B  ·  *multi-select (2 correct)*
**Docs:** https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations

---

## Q503

Which command verifies an artifact attestation from a terminal?

- A. `gh attestation verify <path> --owner <org>`
- B. `gh artifact verify <path>`
- C. `git verify-attestation <path>`
- D. `gh provenance verify <path>`

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/verify-attestations-offline

---

## Q504

How does GitHub document reaching SLSA Build Level 3 with artifact attestations?

- A. Perform the build and attestation inside a reusable workflow that acts as a trusted builder, isolated from the calling workflow
- B. Enable required reviewers on the production environment
- C. Sign the artifact with a personal GPG key after the build
- D. Run the build on larger runners

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/increase-security-rating

---

## Q505

Where can artifact attestations be generated?

- A. Public repositories on any GitHub plan
- B. Private and internal repositories on GitHub Enterprise Cloud
- C. Any repository on GitHub Enterprise Server
- D. Public repositories only if the organization has GitHub Advanced Security

**Answer:** A, B  ·  *multi-select (2 correct)*
**Docs:** https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations

---

## Q506

Why does `gh attestation verify` require an `--owner` or `--repo` flag?

- A. Verification must confirm the attestation was produced by the expected source — the signature alone doesn't prove who built the artifact
- B. The flag is needed to download the artifact from that repository first
- C. It selects which account is billed for the verification API call
- D. It is optional and only speeds verification up

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations

---

## Q507

Which details are recorded in an artifact's build provenance?

- A. The workflow and repository that produced the artifact
- B. The commit SHA the build ran against
- C. The list of CVEs present in the artifact
- D. The names of organization members with repository access

**Answer:** A, B  ·  *multi-select (2 correct)*
**Docs:** https://docs.github.com/en/actions/concepts/security/artifact-attestations

---

## Q508

How do you generate an attestation for a container image rather than a file?

- A. Pass the image's digest to `attest-build-provenance` (`subject-digest`) and verify with an `oci://` reference
- B. Container images cannot be attested
- C. Export the image as a tarball and attest the tarball instead
- D. Enable two-factor authentication on the registry account

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations

---

## Q509

What signs artifact attestations?

- A. Sigstore — the public-good instance for public repositories, GitHub's internal Sigstore instance for private ones
- B. The workflow author's SSH signing key
- C. GitHub staff, manually, on request
- D. The runner's TLS certificate

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/concepts/security/artifact-attestations

---

## Q510

Why is this step a security risk in a workflow triggered by `issues`?

```yaml
- run: echo "Title: ${{ github.event.issue.title }}"
```

- A. The title is attacker-controlled and is substituted into the script before the shell runs, enabling command injection
- B. `echo` automatically leaks secrets to the log
- C. The `issues` event cannot safely trigger workflows at all
- D. `github.event` is not available inside `run:` steps

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/concepts/security/script-injections

---

## Q511

What is the recommended way to use `github.event.pull_request.title` inside a `run:` script?

- A. Assign it to an `env:` variable and reference the quoted variable (e.g. `"$TITLE"`) in the script
- B. Wrap the expression in single quotes inside the script
- C. Escape the expression with backslashes
- D. Only use it on self-hosted runners

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/security/secure-use#use-an-intermediate-environment-variable

---

## Q512

Which of these values are attacker-controllable and must be treated as untrusted input?

- A. `github.event.issue.title`
- B. `github.event.pull_request.body`
- C. `github.head_ref`
- D. `github.workflow`
- E. `github.run_id`

**Answer:** A, B, C  ·  *multi-select (3 correct)*
**Docs:** https://docs.github.com/en/actions/concepts/security/script-injections

---

## Q513

Why does routing untrusted input through an `env:` variable prevent script injection?

- A. The value reaches the process as data in memory instead of being pasted into the script text the shell parses
- B. `env:` values are sanitized by stripping shell metacharacters
- C. `env:` values are encrypted at rest
- D. The shell refuses to expand variables that originate from event payloads

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/security/secure-use#use-an-intermediate-environment-variable

---

## Q514

Besides intermediate environment variables, which are documented mitigations against script injection?

- A. Use an action that consumes the untrusted value as an input, instead of an inline script
- B. Use code scanning (e.g. CodeQL) to detect vulnerable workflow patterns
- C. Base64-encode the untrusted value inside the `run:` script
- D. Move the step to a Windows runner where bash is not the default shell

**Answer:** A, B  ·  *multi-select (2 correct)*
**Docs:** https://docs.github.com/en/actions/reference/security/secure-use#good-practices-for-mitigating-script-injection-attacks

---

## Q515

How does restricting `GITHUB_TOKEN` permissions relate to script injection?

- A. It limits the blast radius — a successful injection only gets the token's reduced scopes
- B. It prevents the injection from executing in the first place
- C. The token is never accessible from `run:` steps anyway
- D. It is unrelated to injection risk

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/security/secure-use#restricting-permissions-for-tokens

---

## Q516

Which combination is the classic "pwn request" vulnerability?

- A. A `pull_request_target` workflow that checks out and executes the pull request's head code
- B. A `pull_request` workflow that builds the merge commit
- C. A `workflow_dispatch` workflow with typed inputs
- D. A `schedule` workflow that reads repository code

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/security/secure-use

---

## Q517

Why doesn't wrapping `${{ github.event.comment.body }}` in single quotes inside `run:` protect against injection?

- A. Actions expands the expression before the shell parses the script, so the payload can include a quote and break out
- B. Single quotes are not valid in bash scripts
- C. It does protect fully — this is the documented mitigation
- D. Comment bodies cannot contain quote characters

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/concepts/security/script-injections

---

## Q518

In a workflow file, what does `&shared` do on the `steps:` line of job `a`?

```yaml
jobs:
  a:
    runs-on: ubuntu-latest
    steps: &shared
      - uses: actions/checkout@v6
      - run: make build
```

- A. Defines an anchor, so the steps list can be reused elsewhere in this file via `*shared`
- B. Creates an alias to a node defined elsewhere
- C. Declares an environment variable named shared
- D. Marks the steps list as deprecated

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/reusing-workflow-configurations#yaml-anchors-and-aliases

---

## Q519

Continuing the previous file, what does `steps: *shared` in job `b` do?

- A. Reuses the exact steps list anchored as `shared` in job `a`
- B. Runs job `a` before job `b`
- C. Imports steps from a file named shared.yml
- D. It is a YAML syntax error

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/reusing-workflow-configurations#yaml-anchors-and-aliases

---

## Q520

Which YAML reuse constructs do GitHub Actions workflow files support?

- A. Anchors (`&`) and aliases (`*`) only — merge keys (`<<`) are a YAML 1.1 feature and Actions follows YAML 1.2
- B. Anchors, aliases, and merge keys
- C. Merge keys only
- D. None — all YAML reuse constructs are rejected by workflow validation

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/reusing-workflow-configurations#yaml-anchors-and-aliases

---

## Q521

What happens when this workflow file is pushed?

```yaml
jobs:
  build:
    runs-on: ubuntu-latest
    env: &base
      LOG_LEVEL: info
      RETRIES: "3"
    steps:
      - run: make build
  test:
    runs-on: ubuntu-latest
    env:
      <<: *base
      LOG_LEVEL: debug
    steps:
      - run: make test
```

- A. The workflow is invalid — GitHub Actions does not support YAML merge keys (`<<:`), only anchors and aliases
- B. It runs with `LOG_LEVEL: debug` in job `test` — the local key overrides the merged one
- C. It runs with `LOG_LEVEL: info` in job `test` — merged keys override local ones
- D. It fails to parse with a duplicate-key error

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/reusing-workflow-configurations#yaml-anchors-and-aliases

---

## Q522

Can `*shared` in one workflow file reference an anchor `&shared` defined in a different workflow file?

- A. No — anchors and aliases are scoped to a single YAML file
- B. Yes, if both files are in `.github/workflows`
- C. Yes, via the `uses:` keyword
- D. Only between a workflow and its reusable workflow

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/reusing-workflow-configurations#yaml-anchors-and-aliases

---

## Q523

Job `b` reuses job `a`'s steps with `steps: *shared`, but needs one extra step at the end. What is true?

- A. An alias reproduces the anchored node exactly and cannot be extended — job `b` needs its own steps list
- B. Extra list items written under the alias are appended automatically
- C. Use `<<:` on the steps sequence to append the extra step
- D. Pass the extra step as a parameter to the alias

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/reusing-workflow-configurations#yaml-anchors-and-aliases

---

## Q524

Which runner does job `deploy` use?

```yaml
jobs:
  build:
    runs-on: &r ubuntu-24.04
    steps:
      - run: make build
  deploy:
    runs-on: *r
    steps:
      - run: make deploy
```

- A. `ubuntu-24.04` — the alias resolves to the anchored value
- B. `ubuntu-latest` — aliases fall back to the default
- C. It fails: aliases are not allowed in `runs-on`
- D. Whatever runner picked up job `build`

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/reusing-workflow-configurations#yaml-anchors-and-aliases

---

## Q525

How does a step add rendered Markdown to the workflow run summary page?

- A. Append Markdown to the file at `$GITHUB_STEP_SUMMARY` (e.g. `echo "## Results" >> "$GITHUB_STEP_SUMMARY"`)
- B. Print `::summary::## Results` to stdout
- C. Write a `summary` key to `$GITHUB_OUTPUT`
- D. Call the run-summaries REST API from the step

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-commands#adding-a-job-summary

---

## Q526

What is `GITHUB_STEP_SUMMARY`?

- A. An environment variable holding the path to a temporary file, unique per step, whose content becomes part of the job summary
- B. A context property containing the text of previous steps' summaries
- C. An automatically created secret
- D. A boolean flag that turns summaries on

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-commands#adding-a-job-summary

---

## Q527

Two steps in the same job each append a heading to their `$GITHUB_STEP_SUMMARY`. What appears on the run summary page?

- A. Both headings — each step's summary content is grouped into the job's summary
- B. Only the last step's content
- C. Only the first step's content
- D. Nothing: only one step per job may write a summary

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-commands#adding-a-job-summary

---

## Q528

What format is job summary content written in?

- A. GitHub-flavored Markdown
- B. Plain text only
- C. HTML only
- D. AsciiDoc

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-commands#adding-a-job-summary

---

## Q529

You want a Markdown test report visible directly on the run page, without downloading artifacts. Which file does the step write to?

- A. `$GITHUB_STEP_SUMMARY`
- B. `$GITHUB_OUTPUT`
- C. `$GITHUB_ENV`
- D. `$GITHUB_PATH`

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-commands#adding-a-job-summary

---

## Q530

What are service containers?

- A. Docker containers that Actions starts for a job (e.g. a database), manages, and destroys when the job ends
- B. The containers all jobs always execute in
- C. Long-running containers shared across workflow runs
- D. Kubernetes pods managed by GitHub

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/use-containerized-services/use-docker-service-containers

---

## Q531

Which runners can host service containers?

- A. Linux runners with Docker installed (e.g. `ubuntu-latest`, or a Linux self-hosted runner with Docker)
- B. All GitHub-hosted runners, including Windows and macOS
- C. Self-hosted runners only
- D. Larger runners only

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/use-containerized-services/use-docker-service-containers

---

## Q532

The job itself runs in a container. How do its steps reach a service container labeled `redis`?

- A. By hostname `redis` — the service label is the hostname on the shared Docker network
- B. Always via `localhost:6379`
- C. Via the service's public IP address
- D. Only through the `job.services.redis.url` context property

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/use-containerized-services/use-docker-service-containers

---

## Q533

The job runs directly on the runner machine (no `container:`). How do steps reach the `redis` service container?

- A. Via `localhost` and a host port mapped with `ports:` (e.g. `6379:6379`)
- B. By hostname `redis`
- C. Via a Docker network alias
- D. They can't — a job container is mandatory for services

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/use-containerized-services/use-docker-service-containers

---

## Q534

Where do you configure Docker health-check settings for a service container?

- A. In the service's `options:` key (`--health-cmd`, `--health-interval`, …)
- B. In a dedicated `healthcheck:` block
- C. In `runs-on`
- D. Health checks are not supported for services

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/use-containerized-services/use-docker-service-containers

---

## Q535

A service maps `ports: ["6379/tcp"]`, letting Docker assign a random host port. How do steps discover the assigned port?

- A. `${{ job.services.redis.ports[6379] }}`
- B. `${{ services.redis.port }}`
- C. The variable `$REDIS_PORT` is set automatically
- D. Randomly assigned ports cannot be discovered

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/use-containerized-services/use-docker-service-containers

---

## Q536

Your organization enables an IP allow list. What does GitHub document about standard GitHub-hosted runners?

- A. They can't be used with the allow list — the ranges are too numerous and updated weekly; use self-hosted runners or larger runners with static IPs
- B. They automatically bypass the allow list
- C. Add the keyword `azure` to the allow list to cover them
- D. IP allow lists do not affect Actions at all

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/runners/github-hosted-runners#ip-addresses

---

## Q537

Which feature provides GitHub-managed runners with fixed IP ranges that can be allow-listed?

- A. Larger runners with static IP addresses
- B. Standard `ubuntu-latest` runners
- C. Runner groups
- D. The `meta` REST API

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/runners/larger-runners

---

## Q538

Where do you obtain the current IP ranges used by GitHub-hosted runners?

- A. The REST API: `GET https://api.github.com/meta` (the `actions` key)
- B. A monthly email from GitHub
- C. The runner's `_diag` folder
- D. They are fixed and listed in the organization settings UI

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/runners/github-hosted-runners#ip-addresses

---

## Q539

Which network access does a self-hosted runner require to receive jobs?

- A. Outbound HTTPS (443) to GitHub only — it polls for jobs; no inbound ports are needed
- B. An inbound HTTPS port reachable from GitHub's webhook IPs
- C. Inbound and outbound SSH (22)
- D. Outbound SMTP for job notifications

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/reference/runners/self-hosted-runners#requirements-for-communication-with-github

---

## Q540

Autoscaled ephemeral self-hosted runners get changing IPs, but the organization uses an IP allow list. What is the documented approach?

- A. Give the runners a stable egress IP range — GitHub recommends a load balancer providing a single IP range — and add it to the allow list
- B. Disable the allow list during business hours
- C. Add `0.0.0.0/0` to the allow list
- D. Ephemeral runners bypass IP allow lists automatically

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/enterprise-cloud@latest/organizations/keeping-your-organization-secure/managing-security-settings-for-your-organization/managing-allowed-ip-addresses-for-your-organization#using-github-actions-with-an-ip-allow-list

---

## Q541

How do you make a self-hosted runner route its traffic through an outbound HTTP proxy?

- A. Set the `https_proxy`/`http_proxy` (and optionally `no_proxy`) environment variables for the runner, or put them in a `.env` file in the runner directory
- B. Pass a `--proxy` flag in the workflow's `runs-on`
- C. Set a `proxy:` key in the workflow YAML
- D. Proxies are not supported by the runner application

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/manage-runners/use-proxy-servers

---

## Q542

At which levels can self-hosted runner groups be created?

- A. Organization and enterprise
- B. Repository, organization, and enterprise
- C. Repository only
- D. Enterprise only

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/manage-runners/self-hosted-runners/manage-access

---

## Q543

By default, can a public repository use an organization's runner group?

- A. No — new runner groups exclude public repositories by default; access must be explicitly enabled
- B. Yes, every repository in the org can use every group
- C. Only if the runners carry a `public` label
- D. Groups have no repository-level access control

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/manage-runners/self-hosted-runners/manage-access

---

## Q544

What is the difference between runner groups and runner labels?

- A. Groups control which repositories/organizations may use the runners; labels route jobs to matching runners via `runs-on`
- B. Groups route jobs; labels control repository access
- C. They are two names for the same mechanism
- D. Labels exist only on GitHub-hosted runners

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/manage-runners/self-hosted-runners/manage-access

---

## Q545

Where does a self-hosted runner write its diagnostic log files?

- A. The `_diag` directory inside the runner application's install directory
- B. `/var/log/github/` on the runner host
- C. The Actions tab of the repository
- D. The system journal only

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/manage-runners/self-hosted-runners/monitor-and-troubleshoot

---

## Q546

How do you check a self-hosted runner's connectivity to GitHub services without running a job?

- A. `config.sh --check --url <repo-url> --pat <token>` (or `config.cmd` on Windows)
- B. `run.sh --diagnose`
- C. `gh runner ping`
- D. Pinging `github.com` is the documented method

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/manage-runners/self-hosted-runners/monitor-and-troubleshoot

---

## Q547

How do you enable verbose debug output for workflow runs?

- A. Set the secret or variable `ACTIONS_STEP_DEBUG=true` (step debug) and `ACTIONS_RUNNER_DEBUG=true` (runner diagnostics)
- B. Add `runs-on: debug` to the job
- C. Pass `--verbose` to `actions/checkout`
- D. Only GitHub support can enable debug logs

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/monitor-workflows/enable-debug-logging

---

## Q548

A workflow that uses a Docker container action fails immediately when moved to `windows-latest`. Why?

- A. Docker container actions run only on Linux runners
- B. `windows-latest` has no shell installed
- C. Docker actions require a personal access token
- D. Docker actions require larger runners

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/concepts/workflows-and-actions/custom-actions#docker-container-actions

---

## Q549

Your JavaScript action works locally but fails in workflows with `Cannot find module 'axios'`. What is the most likely cause?

- A. Actions don't get a dependency install at runtime — the action must ship its dependencies, e.g. bundled into one file with `ncc` (committing `node_modules` also works but is discouraged)
- B. The runner blocks npm traffic
- C. Third-party modules are not allowed in actions
- D. The action declared the wrong shell

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/create-actions/create-a-javascript-action

---

## Q550

A composite action ships a helper script in its own repository. How should a step in the action reference it?

- A. `${{ github.action_path }}/script.sh`
- B. `./script.sh` relative to the consumer's workspace
- C. `$GITHUB_WORKSPACE/script.sh`
- D. `/usr/local/actions/script.sh`

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/create-actions/create-a-composite-action

---

## Q551

How does a JavaScript action signal failure to the runner?

- A. Call `core.setFailed('message')` — it logs an error and sets a failing exit code
- B. Return `false` from the entry function
- C. Write the string `failure` to `$GITHUB_OUTPUT`
- D. Failures can only be signaled from Docker actions

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/tutorials/create-actions/create-a-javascript-action

---

## Q552

Your action's `core.debug()` messages never appear in the workflow log. Why?

- A. Debug messages are shown only when step debug logging is enabled (`ACTIONS_STEP_DEBUG=true`)
- B. `core.debug` is deprecated and does nothing
- C. Debug output is written only to the runner's `_diag` folder
- D. The action must run in a container for debug output

**Answer:** A  ·  *single*
**Docs:** https://docs.github.com/en/actions/how-tos/monitor-workflows/enable-debug-logging

---
